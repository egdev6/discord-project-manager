# v0.2.0

v0.2.0 is an internal fake-first/local baseline for runtime governance. Production readiness is not claimed.

This release promotes the `develop` runtime governance work to `main` after #235 manual verification. It keeps private Discord-to-Engram write/readback deferred under #211.

## Completed scope

### Runtime governance foundation

- #187 / #195 via PR #215 — documented OpenClaw artifact classification, persistence boundaries, private data handling, and secret policy.
- #188 via PR #216 — added Discord request artifact classification fixtures and validators.
- #193 / #194 via PR #217 — added effective runtime context resolution and durable-change audit/provenance contracts.
- #189 / #191 / #192 via PR #218 — defined private runtime backup/restore contracts and validation.
- #206 via PR #219 — pinned validated OpenClaw, Engram, Gentle-AI, and Postgres runtime versions.

### Private Discord-to-Engram governance path

- #211 partial validation via PRs #220-#226 — added fake-first vertical-slice evidence, approval lifecycle, readiness gate, no-op observation design, repair contract, readiness sync, and sanitized live preflight report.
- #227 via PR #229 — packaged deterministic approval guard CLI in the OpenClaw image.
- #228 via PR #230 — packaged synthetic no-op observation CLI and validator.
- #231 via PR #232 — packaged runtime boundary harness and CI image invocation.
- #233 via PR #234 — prepared private no-op rehearsal evidence package.
- #235 via PRs #236-#237 — recorded the private no-op observation pass-summary and clarified QA-07 vs #235 release status.

## Runtime component baseline

This table is the maintained runtime baseline referenced by current validators. The original v0.2.0 validation evidence remains historical; later baseline-update PRs may update this table when the active runtime pin changes.

| Component | Validated default |
| --- | --- |
| OpenClaw base image | `ghcr.io/openclaw/openclaw@sha256:9f55f0cb32b2925a983f40726440189b8f422ec61ae2a0fb0cf90403cf6d63d7` |
| Engram image / CLI source | `ghcr.io/gentleman-programming/engram@sha256:2ac75f5c362570aa438d0543fdf4ec3911ff9412664d79ec9d4f13f3a2678479` |
| Gentle-AI binary | `2.1.11` |
| Postgres | `postgres@sha256:e013e867e712fec275706a6c51c966f0bb0c93cfa8f51000f85a15f9865a28cb` |

Operators may override these pinned defaults intentionally, but unrecorded image overrides are outside this validated baseline. See `docs/operations/runtime-version-baseline.md` for the override policy.

## Validation evidence

Release-promotion validation:

```bash
git diff --check
bash scripts/validate-release-promotion.sh --base origin/main --head origin/develop --force
bash scripts/validate-repo-safe-evidence.sh
bash scripts/validate-runtime-version-baseline.sh
```

Pre-release manual verification for #235 also passed:

- repo-safe validator suite;
- Docker Compose config rendering;
- OpenClaw image build;
- local Postgres/OpenClaw/Engram smoke checks;
- OpenClaw Gentle-AI runtime validation;
- packaged runtime boundary harness;
- private no-op observation rehearsal with summary-minimum evidence.

## Installation and configuration notes

- Use the pinned runtime defaults unless intentionally overriding them in local/private configuration.
- Keep `.env` local and out of git.
- Do not publish real Discord guild/channel/user/message IDs, credentials, screenshots, raw logs, transcripts, private payloads, raw Engram exports, SQL dumps, backup archives, or volume dumps.
- Validate local runtime configuration with:

```bash
docker compose config
bash scripts/validate-runtime-version-baseline.sh
bash scripts/validate-repo-safe-evidence.sh
```

## Migration notes

No data migration is required for the fake-first/local baseline itself.

For private runtime state, use the backup/restore contracts before any destructive restore or rehearsal. Do not restore into an active production-like runtime. Keep rehearsal state isolated from active Compose projects and private volumes unless explicitly approved.

## Rollback and fix-forward notes

If v0.2.0 promotion causes a regression:

1. Keep private data out of public evidence; record only sanitized summaries.
2. Revert the release PR on `main` if the regression is in tracked repository content.
3. If the issue is runtime configuration, restore the previous pinned image/configuration values locally and re-run:

   ```bash
   docker compose config
   bash scripts/validate-runtime-version-baseline.sh
   bash scripts/validate-repo-safe-evidence.sh
   ```

4. If private runtime state is affected, stop write-like Discord actions, use the private backup/restore runbook, and verify recovery with sanitized summaries only.
5. Open a follow-up issue before re-enabling any deferred write/readback path.

## Private Discord-to-Engram status

Historical QA-07 remains recorded as gated in `docs/operations/qa-final-regression-report.md`. For v0.2.0, #235 passed a private no-op observation rehearsal with summary-minimum evidence only.

Validated for v0.2.0:

- pinned runtime component baseline;
- fake-first governance contracts and validators;
- OpenClaw container packaging for approval guard, no-op observation, and runtime boundary harness;
- private no-op Discord observation path returning response-only, ephemeral, no-write behavior.

Deferred beyond v0.2.0:

- live write-like Discord-to-Engram write/readback;
- `approve write` persistence flow;
- production readiness or public Discord readiness.

Issue #211 remains tracked separately for the deferred write/readback slice.

## Known limitations

- Production readiness is not claimed.
- Public Discord readiness is not claimed.
- Live Discord-to-Engram write/readback validation is not claimed.
- Live social publishing, scheduling, analytics ingestion, and Buffer activity are not claimed.
- Deferred capability backlog items before #207 remain outside this release unless explicitly listed above.
